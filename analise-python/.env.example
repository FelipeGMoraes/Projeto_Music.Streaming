# Copie este arquivo para .env e preencha com seus dados locais.
# Nunca versione o arquivo .env (ele já está no .gitignore).

DB_SERVER=localhost\\SQLEXPRESS
DB_DATABASE=Music.Streaming

# Autenticação SQL (opcional). Deixe em branco para Windows Authentication.
DB_USER=
DB_PASSWORD=
